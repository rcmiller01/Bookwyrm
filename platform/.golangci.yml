run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gosimple
    - gocritic
    - misspell
    - exportloopref

issues:
  exclude-use-default: true
  max-issues-per-linter: 50
  max-same-issues: 5
